import React from "react";
/**
 * 프로필 추가하는 함수만드는 중입니다.
 * @param {*} user
 */
function addProfile(user) {
  //  파일이름은 multer 라이브러리가 자동으로 겹치지않는 코드로 만들어 전송해둔다.
  // const multer = require("multer");
  // const upload = multer({ dest: `/users/${user._id}` });
  // app.use("/image", express.static(`/users/${user._id}`));
  // app.post("/api/user", upload.single("image"), (req, res) => {
  //   let sql = "INSERT INTO CUSTOMER VALUES (null,~~)";
  //   let image = "/image/" + requestAnimationFrame.file.filename;
  // });
}
export default addProfile;
